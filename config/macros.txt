# macros.txt：插件 macros 「脚本」设定文件
#
# 请注意此档只能是 UTF-8 编码，DOS 断行格式，无 BOM，否则宏会加载失败。
# 若要编辑此文件，请使用支持转换文字编码的文书编辑器，例如：MadEidt。
#
# 使用MACRO脚本，您必须先安装 macros 这个 plugins 扩充套件。
# 详情请自行上网查相关教学，仙境外挂研究网 http://www.openkore.cn
# 若无安装扩充套件 macros，则此档没有作用。
#
# （macros）是一种 script 脚本语言，可以用来写小型的描述式，
# 用途是补足 kore AI 的不足，让外挂可以做更多事情。
# 脚本以区块组成，一个区块就是一个程序代码。
# 每个区块有自己的自定义macro名称。
#
# #字符号开头者，该行为批注。批注不会被执行。

############################
#     档案内容由此开始     #
############################

# 以下提供一些简易宏范例，若要使用请消除前面批注符号，存盘后重启即可使用。
# 若觉得碍眼，或怀疑是否不安全，可从此行至档尾为止的部份删除。
